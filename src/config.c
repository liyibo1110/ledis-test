#include "redis.h"

/**
 * 从文件和option中载入配置
 */ 
void loadServerConfig(char *filename, char *option){

    sds config = sdsempty();
    char buf[REDIS_CONFIGLINE_MAX + 1];

    if(filename){

    }

    if(option){
        
    }
}